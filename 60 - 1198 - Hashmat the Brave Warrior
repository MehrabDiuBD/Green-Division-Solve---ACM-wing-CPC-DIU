#include <stdio.h>

int main() {

    long long int n2, n1;
    while(scanf("%lld", &n1) != EOF) {

        scanf("%lld", &n2);

        if(n1>n2) printf("%lld\n", n1-n2);

        else printf("%lld\n", n2-n1);
    }

    return 0;
}
