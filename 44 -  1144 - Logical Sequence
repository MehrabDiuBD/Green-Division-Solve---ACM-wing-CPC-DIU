#include <stdio.h>

int main() {

    int N;
    scanf("%d", &N);

    for(int i=1; i<=N; i++) { //int k=i;

        for(int j=1; j<=2; j++)

        if(j==1) printf("%d %d %d\n", i, i*i, i*i*i);
        else if(j==2) printf("%d %d %d\n", i, (i*i)+1, (i*i*i)+1);
    }

    return 0;
}
