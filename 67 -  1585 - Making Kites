#include <stdio.h>
 
int main() {
 
    int nT;
    scanf("%d", &nT);
    
    for(int i=1; i<=nT; i++) {
        
        int n1, n2, area;
        scanf("%d %d", &n1, &n2);
        
        area = n1*n2*0.5;
        
        printf("%d cm2\n", area);
    }
    
    return 0;
}
