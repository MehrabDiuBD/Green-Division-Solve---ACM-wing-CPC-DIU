#include <stdio.h>
 
int main() {
 
    int nT;
    scanf("%d", &nT);
    
    for(int i=1; i<=nT; i++) {
        
        int ni;
        scanf("%d", &ni);
        
        if(ni%2==0) printf("0\n");
        else printf("1\n");
    }
 
    return 0;
}
