#include <stdio.h>
 
int main() {
 
    int X, Y;
    scanf("%d %d", &X, &Y);
    
    int smallest = (X<Y) ? X : Y;
    int largest = (X>Y) ? X : Y; 
    
    for(int i=smallest+1; i<largest; i++) {
        
        if(i%5==2 || i%5==3) printf("%d\n", i);
    }
 
    return 0;
}
